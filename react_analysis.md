# 智能客服是否需要 ReAct 范式分析

## 本项目的架构：固定流水线

```
意图分类 → 路由到 Handler → Handler 内编排好的步骤 → 生成回复
```

每个 Handler 里的逻辑是写死的：

- `OrderQueryHandler`：先 tool call → 拿结果 → 再 LLM 生成回复（两步固定流程）
- `PolicyHandler`：先 RAG 检索 → 拼 prompt → LLM 生成（两步固定流程）
- `ProductInquiryHandler`：先查商品 → 拼 prompt → LLM 生成（两步固定流程）

LLM 不能自主决定"我需要再调一次工具"或"我换个工具试试"。

## ReAct 是什么

ReAct = **Re**asoning + **Act**ing，LLM 自己循环思考和行动：

```
思考：用户问的是订单退款，我先查订单状态
行动：call get_order_details(order_id=xxx)
观察：订单状态是已发货
思考：已发货才能退款，我再查一下退款政策
行动：call query_policy_kb("已发货退款流程")
观察：政策说需要先申请退货...
思考：信息够了，可以回答了
回答：...
```

关键区别：**LLM 自己决定下一步做什么**，而不是写死流程。

## 固定流水线 vs ReAct 对比

| 场景 | 固定流水线（现在） | ReAct |
|---|---|---|
| "我的订单到哪了" | 够用 | 杀鸡用牛刀 |
| "我想退款，但不知道流程，我的订单是 xxx" | 勉强够用 | 更自然 |
| "这个商品跟另一个比怎么样，退换货政策是什么" | 处理不了 | 能处理 |
| 跨意图的复杂问题 | 搞不定 | 能搞定 |

## 结论：本项目不需要 ReAct

原因：

- 客服场景大部分是单一意图的简单问题
- 意图分类 + Handler 编排已经够用
- ReAct 会增加 token 消耗、延迟和不确定性

## 什么时候值得上 ReAct

- 工具数量多（十几个以上），LLM 需要自己判断调哪些
- 问题复杂度高，用户一句话涉及多个意图
- 需要多步推理，每步结果决定下一步怎么做

面试时能讲清楚"我的场景用固定流水线就够，ReAct 适合更复杂的场景"，比硬套 ReAct 更加分。
